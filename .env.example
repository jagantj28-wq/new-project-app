# FarmTwin Environment Configuration
# Optional: GEMINI_API_KEY for extended AI Agronomist Copilot reasoning.
# The app operates with high precision even without an API key via its built-in agronomic expert engine.
GEMINI_API_KEY=
PORT=8000
HOST=127.0.0.1
DEBUG=True
